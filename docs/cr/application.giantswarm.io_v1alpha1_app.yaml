apiVersion: application.giantswarm.io/v1alpha1
kind: App
metadata:
  annotations:
    giantswarm.io/docs: https://pkg.go.dev/github.com/giantswarm/apiextensions/pkg/apis/application/v1alpha1?tab=doc#App
  creationTimestamp: null
  name: prometheus
spec:
  catalog: my-catalog
  config:
    configMap:
      name: my-configmap
      namespace: monitoring
    secret:
      name: my-secret
      namespace: monitoring
  kubeConfig:
    context:
      name: my-context-name
    inCluster: false
    secret:
      name: my-kubeconfig-secret
      namespace: monitoring
  name: prometheus
  namespace: monitoring
  userConfig:
    configMap:
      name: my-userconfig-configmap
      namespace: monitoring
    secret:
      name: my-userconfig-secret
      namespace: monitoring
  version: 1.0.0
