version: 2.1

orbs:
  architect: giantswarm/architect@dev:support-dep

workflows:
  workflow:
    jobs:
      - architect/go-test-legacy:
          name: test
      - architect/go-architect:
          name: build
          command: build
          requires:
            - test
      - architect/go-architect:
          name: deploy
          command: deploy
          requires:
            - build
          filters:
            branches:
              only: master
